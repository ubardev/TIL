let name = 'apple';
let color = 'red';
let display = 'redApple';
let orangeName = 'orange';

let apple = {
  name: 'apple',
  color: 'red',
  display: 'redApple',
};

console.log(apple);
console.log(apple.name);
console.log(apple.color);
console.log(apple.display);

let orange = {
  name: 'orange',
  color: 'orange',
  display: 'orangeOrange',
};

// code
// data
// stack
// heap

// 원시 primitive는 Data, Stack에 들어감
// 객체는 heap에 들어감
